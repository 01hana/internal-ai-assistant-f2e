# Server-only upstream for Nuxt/Nitro's same-origin Assistant SDK proxy.
# SDK browser traffic stays on /api/v1 and does not need this origin.
# Frontend 001 keeps its existing NUXT_API_URL / runtimeConfig.public.apiBase ownership.
# Local development may point this at the local Gateway.
ASSISTANT_API_ORIGIN=http://localhost:4000
